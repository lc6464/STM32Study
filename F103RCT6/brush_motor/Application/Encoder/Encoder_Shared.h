#pragma once

#include "Encoder.h"

// 四个编码器
extern Encoder leftFrontEncoder;  // 左前轮编码器
extern Encoder rightFrontEncoder; // 右前轮编码器
extern Encoder leftBackEncoder;   // 左后轮编码器
extern Encoder rightBackEncoder;  // 右后轮编码器